Prefer the domain glossary business names when available.
Keep every definition under 240 characters.
Tag every enriched column with `tutorial`.
Treat `national_id` and `email` as sensitive personal data in wording tone,
but do not change privacy classification unless evidence requires it.
