# Pip constraints for redibis [dev] / [all] — mirrors docker/local/constraints.txt.
# Use: pip install -e ".[dev]" -c requirements/constraints.txt
#
# Prevents resolver backtracking (torch, httpx, sqlalchemy, pyspark sdist rebuilds).

sqlalchemy>=1.4.52,<2.0
httpx>=0.27,<0.28
pydantic>=2.0,<3.0
# GE 0.17 + CDP stack
great-expectations==0.17.23
# Golden vector index (default in-memory backend)
faiss-cpu>=1.7,<1.10
numpy>=1.24,<2.0
# GLiNER transitive — avoid pulling latest heavy torch/transformers builds
torch>=2.1.0,<2.4.0
transformers>=4.40.0,<4.42.0
tokenizers>=0.19,<0.20
# litellm: >=1.75 needs httpx>=0.28; collides with JupyterLab pin above
litellm>=1.55,<1.75
# redibis[mask]
ff3>=1.0
cryptography>=42.0
faker>=24.0
rstr>=3.0
# MSISDN plan validation (core dep; bundled metadata — air-gap safe)
phonenumbers>=8.13,<10
